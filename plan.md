# Plan: prj-3444-task-muh4jppf

**Project:** `PRJ-9620`  
**Task ID:** `task-muh4jppf`  
**Repo:** `prj-3444-task-muh4jppf`  

## Summary

Декомпозирую задачу на 3 этапа: структура репозитория → реализация скрипта мониторинга CPU/RAM → README и smoke-тест.

## Subtasks

### 1. Создать структуру репозитория

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Инициализировать репозиторий prj-3444-task-muh4jppf: создать monitor.py (заглушка с shebang и docstring), requirements.txt с зависимостью psutil, .gitignore для __pycache__/.venv, README.md с описанием и примером запуска.
- **Test plan:** Файлы monitor.py, requirements.txt, .gitignore, README.md существуют; requirements.txt содержит psutil; python3 -m py_compile monitor.py проходит без ошибок.

### 2. Реализовать сбор метрик CPU и памяти

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** В monitor.py реализовать функции get_cpu_usage() и get_memory_usage() через psutil, формат вывода (процент CPU, used/total RAM в MB и процентах), CLI-режим с argparse (флаги --interval, --json, --once), корректное завершение по Ctrl+C.
- **Test plan:** python3 monitor.py --once выводит валидные значения CPU% и RAM; python3 monitor.py --json отдаёт парсимый JSON; --interval 1 работает 5 секунд и корректно прерывается по SIGINT.
- **Dependencies:** `sub-1`

### 3. Написать smoke-тесты и финализировать README

- **ID:** `sub-3`
- **Profile:** `round-robin`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить test_monitor.py с базовыми проверками (CPU в [0,100], RAM used<=total, JSON парсится); дополнить README секциями Usage с примерами всех флагов и Testing; убедиться что git status чист и есть diff для передачи ревьюеру.
- **Test plan:** python3 -m unittest test_monitor.py -v проходит; README содержит блоки Usage/Testing; git diff --stat показывает monitor.py, test_monitor.py, requirements.txt, README.md.
- **Dependencies:** `sub-2`


---
*Generated by Hermes Orchestrator*
